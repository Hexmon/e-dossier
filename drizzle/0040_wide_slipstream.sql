-- This journaled migration was generated as a duplicate rollup of earlier
-- curated migrations. Those migrations already create the tables, columns,
-- constraints, indexes, and enum values that were previously repeated here.
--
-- Keep the file as an explicit no-op so Drizzle can advance the journal entry
-- without reapplying existing DDL on on-prem deployments.
SELECT 1;
