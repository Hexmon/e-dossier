export const baseURL = 'http://localhost:3000/api/v1'

export const endpoints = {
    auth: {
        login: "/auth/login",
        signup: "/auth/signup"
    },
    admin: {
        approval: "/admin/signup-requests",
    }
}