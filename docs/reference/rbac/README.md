# RBAC Reference

This folder is reserved for RBAC reference documentation and machine-readable RBAC support assets.

Expected contents include:

- parsed permission matrix outputs
- API and page surface inventories
- RBAC reference notes when they are added

Canonical machine-readable path prefix:

- `docs/reference/rbac/`
