# ZAP baseline rule configuration.
# Keep this narrow: unlisted/new warnings remain WARN and fail zap-baseline.
# Rule 10055 is accepted as INFO for the current Next.js production build because
# Next's inline bootstrap/RSC payload requires script-src 'unsafe-inline' until a
# nonce-based CSP is implemented. The app still sends a CSP header in production.
10049	INFO	(Cacheable content accepted for hashed Next.js static assets)
10055	INFO	(CSP unsafe-inline accepted for current Next.js bootstrap; revisit before strict nonce CSP rollout)
10109	INFO	(Modern web application informational finding)
